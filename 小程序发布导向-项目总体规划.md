# 摇骰子项目（微信小程序发布导向）总体规划

## 1. 最终目标（先定死）

项目最终交付形态：**微信小程序可正式发布版本**（非仅H5 Demo）。

发布目标：
- 可通过微信小程序审核并上线
- 支持真实用户房间对战（2-12人）
- 核心玩法闭环稳定可用（建房/摇骰/叫牌/开牌/结算/再来一局）
- 具备基础运营能力（日志、告警、版本回滚）

## 2. 技术路线（按小程序约束选型）

## 2.1 前端（推荐）

优先方案：**微信原生小程序 + TypeScript**
- 原生 API 兼容最好，提审风险最低
- 实时、音频、权限、分享链路可控
- 性能和包体可针对小程序优化

备选方案（仅在明确多端诉求时）：Taro/uni-app  
- 可提升多端复用
- 但需要额外处理小程序端适配和性能细节

## 2.2 后端（必须独立）

- Node.js + WebSocket（原生 ws 协议，避免重度依赖浏览器端特性）
- Redis（房间态、会话、限流、幂等）
- MongoDB/MySQL（二选一，存历史与运营数据）
- Nginx/WSS（TLS 证书，稳定长连）

说明：小程序端使用 `wss` 长连接，服务端做权威状态机判定。

## 2.3 CI/CD（面向发布）

- 开发环境：微信开发者工具 + 真机调试
- 构建发布：`miniprogram-ci` 自动上传体验版
- 发布流程：开发版 -> 体验版 -> 提审版 -> 线上版

## 3. 产品范围（V1 按上线最小闭环）

V1 必做：
- 登录与用户初始化（微信授权最小化）
- 房间创建/加入/离开
- 2-12 人动态座位与状态展示
- 摇骰、叫牌、开牌、结算
- 本局历史记录（结算弹层 + 更多入口）
- 断线重连与状态恢复

V1 暂缓：
- VIP 概率加成（影响公平与提审风险）
- 广告变现、内购链路
- 复杂社交（好友、排行榜）

## 4. 小程序发布关键约束（必须前置）

1. 网络与域名
- API 与 WebSocket 域名必须在小程序后台配置合法域名
- 线上必须 HTTPS/WSS，有有效证书
- 服务器需满足备案与稳定性要求

2. 权限与隐私
- 使用麦克风需 `scope.record`，并提供明确用途说明
- 上线前必须完善《隐私政策》与用户协议
- 用户数据采集最小化，提供注销/删除说明

3. 审核内容风险（本项目重点）
- 禁止出现赌博导向文案或玩法（下注、现金奖池、兑付）
- 禁止诱导酒精类危险行为的表述
- 页面和文案统一为“休闲娱乐工具”，不涉及真钱输赢

4. 包体与性能
- 控制主包体积，重资源分包
- 动画特效不做重 3D 引擎，优先轻量动画实现

## 5. 架构原则（防返工）

1. 服务端权威
- 客户端只发动作意图，不做胜负最终判定
- 随机数与规则计算在服务端执行并签名记录

2. 协议先行
- 先冻结 Socket 事件名和 DTO，再并行前后端
- 全部动作带 `actionId` 做幂等
- 状态快照带 `version` 与 `serverTs` 防乱序

3. 小程序优先 UI 设计
- 先保证操作清晰和帧率稳定，再叠加特效
- 12 人极限布局真机验证优先于设计稿还原

## 6. 里程碑计划（发布导向）

M1（第1-2周）：可连接房间
- 房间页骨架、2-12 人布局、基础状态同步

M2（第3-4周）：玩法闭环打通
- 摇骰/叫牌/开牌/结算/再来一局
- 断线重连、异常处理

M3（第5周）：提审准备
- 隐私协议页、权限文案、日志告警、体验版内测
- 修复真机兼容性问题（iOS/Android）

M4（第6周）：提交审核与发布
- 提审材料完善
- 审核反馈修复
- 灰度放量上线

## 7. 测试与验收标准（按上线要求）

功能验收：
- 2/4/6/8/12 人房间都能完整进行一局
- 开牌前不可见他人明文点数
- 断线重连后房间状态一致

性能验收：
- 主流程操作响应 < 300ms（不含网络波动）
- 房间状态同步延迟可控（目标 < 200ms 局域网，< 500ms 公网）

稳定性验收：
- 连续对局 100 局无严重状态错乱
- 高峰并发压测下无大面积掉线

审核验收：
- 敏感文案、权限弹窗、隐私协议全部通过自检

## 8. 当前决策建议（立刻执行）

1. 立刻冻结“微信小程序上线”作为唯一 V1 目标。  
2. 立刻删除/下线所有“下注、筹码、现金输赢”相关文案与交互。  
3. 立刻把“房间模块V1”按小程序 API 约束改造（语音、分享、网络、权限）。  
4. 商业化放到 V2，在 V1 上线稳定后再接入。  

