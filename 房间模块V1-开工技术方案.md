# 摇骰子项目 - 房间模块V1开工技术方案（微信小程序发布导向）

> 本文档默认以“微信小程序最终可审核发布”为前提，技术与交互决策均优先满足小程序约束。
> 历史技术方案口径说明：本文是房间模块早期开工方案，部分内容仍保留“2-12 人动态布局”等历史设想。当前发布版本以“固定 8 杯位房间”为准；最新房间逻辑、退出/断线、旁观入座和 UI 规范见 `docs/2026-04-23-发布前房间逻辑与UI沉淀.md`。

## 1. 范围定义（V1）

目标：先交付“可玩闭环”，不在V1引入VIP概率加成、广告、商城交易。

V1包含：
- 建房/入房（2-12人）
- 房间内动态座位布局
- 摇骰（每人每轮1次，5颗）
- 叫牌（X个Y）
- 开牌（单开/多开）
- 结算（按规则计算胜负）
- 本局历史记录（结算后可查看）

V1不包含：
- VIP概率影响结果
- 付费、广告激励、商城购买链路
- 复杂社交系统（好友、排行榜）

## 2. 统一规则基线（需在开工前冻结）

建议冻结以下规则，避免前后端各自理解：

1. 叫牌合法性  
- 叫牌格式：`count` + `point`（如 `3个6`）  
- 下家叫牌必须“严格大于”上家  
- 比较方式建议：先比 `count`，若相等再比 `point`（可配置）

2. 摇骰可见性  
- 摇骰阶段：仅玩家本人可见自己的 `diceResult`  
- 未开牌前，任何玩家看不到他人明文点数  
- 开牌后：全员可见全员结果

3. 开牌判定  
- 开牌方可指定1..N个目标（不含自己）  
- 对每个目标独立判定胜负  
- 判定依据：目标声明 vs 实际统计（含1点是否通配，需配置开关）

4. 回合推进  
- 每轮每人只能摇1次  
- 叫牌结束后进入开牌  
- 结算完成可“再来一局”

## 3. 后端权威状态机

房间状态（RoomPhase）：
- `ready`
- `diceRolling`
- `calling`
- `opening`
- `ended`

状态迁移：
1. `ready -> diceRolling`：房主点击开始且人数 >= 2  
2. `diceRolling -> calling`：所有在线玩家都完成摇骰  
3. `calling -> opening`：有玩家发起开牌  
4. `opening -> ended`：胜负判定完成  
5. `ended -> diceRolling`：再来一局

动作权限矩阵（核心）：
- `rollDice`：仅 `diceRolling` 且 `actorId == currentPlayerId`
- `makeCall`：仅 `calling` 且 `actorId == currentPlayerId`
- `openDice`：仅 `calling`，可由当前可操作玩家触发
- `restartRound`：仅 `ended`，房主或投票通过

## 4. 服务端事件协议（Socket）

客户端 -> 服务端：
- `room:create` `{nickname, avatar}`
- `room:join` `{roomId, nickname, avatar}`
- `room:leave` `{}`
- `game:start` `{}`
- `dice:roll` `{}`
- `call:make` `{count:number, point:number}`
- `open:request` `{targetPlayerIds:string[]}`
- `round:restart` `{}`
- `voice:transcript` `{text:string, ts:number}` (可选)

服务端 -> 客户端：
- `room:state` `RoomStateDTO`（全量快照）
- `room:delta` `RoomDeltaDTO`（增量更新）
- `action:ack` `{actionId, ok, reason?}`
- `dice:privateResult` `{roundId, playerId, dice:number[]}`（仅本人）
- `open:result` `OpenResultDTO`
- `round:summary` `RoundSummaryDTO`
- `system:error` `{code, message}`

建议：
- 所有客户端动作附 `actionId`（幂等）
- 服务端返回 `serverTs` 与 `version`（便于乱序处理）

## 5. 数据结构（统一版）

```ts
type OnlineStatus = 'online' | 'offline';
type TurnStatus = 'idle' | 'active' | 'done';
type DiceCupStatus = 'closed' | 'open';
type RoomPhase = 'ready' | 'diceRolling' | 'calling' | 'opening' | 'ended';

interface Call {
  count: number;
  point: number; // 1..6
  by: string; // playerId
  ts: number;
}

interface PlayerState {
  id: string;
  nickname: string;
  avatar: string;
  isOwner: boolean;
  onlineStatus: OnlineStatus;
  turnStatus: TurnStatus;
  seatIndex: number;
  diceCupStatus: DiceCupStatus;
  currentCall?: Call;
  // 注意：diceResult 在未开牌阶段不进入公开快照
}

interface RoomStateDTO {
  roomId: string;
  phase: RoomPhase;
  round: number;
  currentPlayerId?: string;
  players: PlayerState[];
  lastCall?: Call;
  networkHealth: 'good' | 'lag' | 'disconnected';
  version: number;
  serverTs: number;
}

interface OpenResultDTO {
  round: number;
  openerId: string;
  targets: Array<{
    targetId: string;
    declared: { count: number; point: number };
    actual: number;
    winnerId: string;
  }>;
}
```

## 6. 前端页面与组件树（房间页）

RoomPage
- TopBar
  - UserBadge（自己/房主）
  - RoomIdChip
  - NetworkChip
  - MoreButton
- TableStage
  - SeatRing（2-12人动态布局）
    - PlayerSeat（头像、状态边框、骰盅）
    - CallBubble（仅最新叫牌）
  - CenterDicePanel（摇骰动画/开牌结果）
- RightDrawer（可折叠）
  - CallTimeline（完整叫牌记录）
- BottomActionBar
  - VoiceButton（按住说话）
  - RollButton
  - OpenButton
  - MoreButton
- Modals
  - OpenTargetModal
  - RuleModal
  - RoundSummaryModal
  - HistoryModal

## 7. 动态布局实现要点

基础公式沿用文档圆形均分；补充三点：

1. 防重叠  
- 气泡宽度上限 + 文案截断  
- 邻近座位气泡错层（y轴轻微偏移）

2. 安全边距  
- 外圈半径计算加入 `safePadding`，避免刘海屏/圆角裁切

3. 过渡动画  
- 人数变化、重排使用 300-500ms transform 过渡
- 严禁直接改 top/left 触发布局抖动

## 8. 历史记录入口策略

推荐“双入口”：
- 主入口：`更多 -> 游戏历史`（不占用主操作位）
- 高意图入口：`RoundSummaryModal -> 查看本局历史`

记录内容：
- 回合号、玩家叫牌链、开牌关系、胜负结果、每人最终点数

## 9. 工程拆分与排期（建议2周一个迭代）

迭代1（房间骨架）：
- 房间连接、Top/Center/Bottom布局、2-12人座位算法、基础状态同步

迭代2（核心玩法）：
- 摇骰、叫牌、开牌、判定、回合流转、结算弹层

迭代3（体验完善）：
- 语音输入、历史记录、断线重连、动画和音效优化

迭代4（商业化接入）：
- 商城、VIP展示、广告入口（仅UI和账户，不改胜负概率）

## 10. 关键风险与规避

1. 规则歧义导致返工  
- 先冻结规则文档，前后端共签字

2. 客户端判定导致作弊  
- 胜负判定和随机数只在服务端

3. 高并发乱序  
- 事件版本号 + 幂等 actionId + 服务端时钟

4. 布局拥挤  
- 12人极限布局必须真机验证（小屏安卓优先）

## 11. 开工检查清单

- [ ] 规则冻结（叫牌比较、1点通配、开牌多目标）
- [ ] Socket事件名与DTO冻结
- [ ] 状态机与权限矩阵评审通过
- [ ] RoomPage组件树确认
- [ ] 2-12人布局真机验收标准确定
